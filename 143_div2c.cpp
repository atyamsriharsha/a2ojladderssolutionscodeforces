#include <bits/stdc++.h>
using namespace std ;
int main()
{
	long long int n,k,temp ;
	cin >> n >> k ;
	std::vector<long long int> v;
	for(long long int i=0;i<n;i++)
	{
		cin >> temp ;
		v.push_back(temp) ;
	}
	
	return 0 ;
}